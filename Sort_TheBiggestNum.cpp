//https://www.welcomekakao.com/learn/courses/30/lessons/42746

#include <string>
#include <vector>
#include <iostream>
#include<algorithm>

using namespace std;

/*
bool comp  (int a,  int b);

string solution(vector<int> numbers) {
    
    string answer = "";
    sort(numbers.begin(),numbers.end(),comp);
    if(numbers[0]==0)return "0";
    
    for(int i=0;i<numbers.size(); i++)
        answer = answer + to_string(numbers[i]);
    
    string answers = "";
    answers=to_string(stoi(answer));
    return answers;
}


 bool comp  (int a,  int b)
{
    string aa = to_string(a);
    string bb = to_string(b);
    
   bool result = (stoi(string(aa+bb))<stoi(string(bb+aa)))? false: true;
     
    return result;
     
}
*/

string solution(vector<int> numbers) {
    
    string answer = "";
    
    
    
    return;
}
